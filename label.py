def label_address(address):
    """
    Given un unstructured address, return an address dictionary with corresponding labels
    Ideally, use address labeling API using Conditional Random Fields to label the address, 
    such as the libpostal API
    """
    # Currently, I am having trouble with installing libpostal and getting
    # the dependencies/datasets to connect
    pass
